server:
  port: 8080

spring:
  profiles:
    active: dev
  main:
    allow-circular-references: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/sky_take_out?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root
    password: 1234
#  redis:
#    host: ${saturday.redis.host}
#    port: ${saturday.redis.port}
#    password: ${saturday.redis.password}
#    database: ${saturday.redis.database}

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.saturday.entity
  configuration:
    map-underscore-to-camel-case: true

logging:
  level:
    com:
      saturday:
        mapper: debug
        service: info
        controller: info

saturday:
  jwt:
    admin-secret-key: saturday-cream-233-1234567890abcdef
    admin-ttl: 7200000
    admin-token-name: token

aws:
  bucket: ${saturday.aws.s3.bucket}
  region: ${saturday.aws.s3.region}

  access-key: ${saturday.aws.credentials.access-key}
  secret-key: ${saturday.aws.credentials.secret-key}